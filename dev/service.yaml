apiVersion: v1
kind: Service
metadata:
  name: gitops-service
spec:
  selector:
    app: gitops
  type: LoadBalancer  
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30001